<h2>Show company</h2>

<ul>

  <li>
    <strong>Name:</strong>
    <%= @company.name %>
  </li>

</ul>

<%= link "Edit", to: company_path(@conn, :edit, @company) %> |
<%= link "Show Offices", to: office_path(@conn, :index, %{company_id: @company.id}) %> |
<%= link "Show Departments", to: department_path(@conn, :index, %{company_id: @company.id}) %> |
<%= link "Show Positions", to: position_path(@conn, :index, %{company_id: @company.id}) %> |
<%= link "Back", to: company_path(@conn, :index) %>
