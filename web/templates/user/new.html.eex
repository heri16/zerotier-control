<h2>New tenant</h2>

<%= render "form.html", changeset: @changeset,
                        tenants: @tenants,
                        action: user_path(@conn, :create) %>

<%= link "Back", to: user_path(@conn, :index) %>
